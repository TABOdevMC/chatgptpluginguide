// Deuxième couche de contenu pédagogique pour les parcours restants.
(function () {
  'use strict';

  const EXTRA = {
    data: [
      ['La mémoire vive est adaptée aux données temporaires et aux caches. Une Map<UUID, PlayerData> permet un accès direct aux données d’un joueur. Pour une vraie persistance, il faut ensuite synchroniser cette mémoire avec un stockage durable.','Map<UUID, PlayerData> players = new HashMap<>();\nPlayerData data = players.computeIfAbsent(uuid, PlayerData::new);','Ne jamais traiter une HashMap comme une sauvegarde : un redémarrage détruit son contenu.'],
      ['Le PersistentDataContainer est pensé pour associer une donnée technique à un objet Paper compatible. Pour un item custom, stocker un identifiant dans son ItemMeta permet de reconnaître l’objet même si son nom ou son lore change.','NamespacedKey key = new NamespacedKey(plugin, "item_id");\nmeta.getPersistentDataContainer().set(key, PersistentDataType.STRING, "magic_sword");','Comparer uniquement le nom affiché de l’item est fragile : le texte visible peut être modifié.'],
      ['Une NamespacedKey doit être stable dans le temps. Si tu changes arbitrairement la clé d’une donnée après publication, les anciennes données seront toujours stockées sous l’ancien identifiant et ton plugin ne les retrouvera plus.','NamespacedKey key = new NamespacedKey(plugin, "coins");\nInteger coins = player.getPersistentDataContainer().get(key, PersistentDataType.INTEGER);','Changer une clé utilisée en production sans migration peut rendre des données existantes invisibles.'],
      ['La metadata décrit surtout un état temporaire du runtime. Elle est pratique pour savoir qu’un joueur est en combat ou qu’une entité a reçu un traitement, mais elle ne remplace pas un modèle de données persistant.','player.setMetadata("in-combat", new FixedMetadataValue(plugin, true));\nboolean combat = player.hasMetadata("in-combat");','Utiliser Metadata pour sauvegarder une monnaie ou une progression entre redémarrages.'],
      ['YAML est lisible par un humain, ce qui le rend excellent pour la configuration et de petits fichiers. En revanche, écrire un très gros fichier à chaque petite modification devient coûteux et difficile à maintenir.','config.set("players." + uuid + ".coins", coins);\nsaveConfig();','Sauvegarder tout le fichier après chaque clic ou événement fréquent.'],
      ['PlayerData doit représenter l’état métier du joueur, pas la totalité du comportement du serveur. Garde ses champs cohérents et expose des méthodes qui maintiennent les invariants, par exemple empêcher un nombre de coins négatif.','public void removeCoins(int amount) {\n    if (amount < 0 || coins < amount) throw new IllegalArgumentException();\n    coins -= amount;\n}','Mettre toute la logique du plugin dans PlayerData et en faire un objet « fourre-tout ».'],
      ['Un repository masque le détail du stockage. Le service n’a alors pas besoin de savoir si les données viennent d’un YAML, d’un fichier JSON ou d’une base SQL. Cette séparation facilite aussi une future migration vers SQLite ou MySQL.','public interface PlayerRepository {\n    PlayerData load(UUID uuid);\n    void save(PlayerData data);\n}','Faire dépendre directement chaque listener d’une implémentation SQL spécifique.'],
      ['Choisis le stockage selon le cycle de vie, le volume et le partage nécessaire. Une configuration n’a pas les mêmes contraintes qu’une économie de plusieurs centaines de milliers de joueurs. Pour un réseau, les écritures concurrentes et la cohérence deviennent également importantes.','Runtime cache → HashMap\nObjet Paper → PDC\nPetites données → YAML\nDonnées structurées → SQLite / MySQL','Choisir une technologie uniquement parce qu’elle est populaire sans regarder le volume et les besoins du plugin.']
    ],
    items: [
      ['ItemStack représente l’état d’une pile d’objets. Sa quantité, son matériau et ses métadonnées sont séparés. Commence par maîtriser cette structure avant de construire des objets custom complexes.','ItemStack sword = new ItemStack(Material.DIAMOND_SWORD);\nsword.setAmount(1);','Créer un item avec un constructeur personnel alors que Paper fournit déjà ItemStack.'],
      ['ItemMeta contient les propriétés supplémentaires de l’item. Selon la version Paper, privilégie les méthodes modernes basées sur Adventure pour le texte affiché.','ItemMeta meta = sword.getItemMeta();\nmeta.displayName(Component.text("Épée du gardien"));\nsword.setItemMeta(meta);','Modifier le ItemMeta puis oublier item.setItemMeta(meta).'],
      ['Le PDC donne une identité technique à ton item. Tu peux ainsi avoir plusieurs épées qui portent le même nom mais des comportements différents selon un id stocké dans leur meta.','meta.getPersistentDataContainer().set(\n    itemKey,\n    PersistentDataType.STRING,\n    "guardian_sword"\n);','Utiliser le slot ou la couleur comme identifiant : ces propriétés ne sont pas uniques.'],
      ['Un événement d’interaction peut fournir un item absent, une action inattendue ou une interaction dans une autre main. Un bon code filtre immédiatement les conditions qui ne correspondent pas au comportement recherché.','ItemStack item = event.getItem();\nif (item == null) return;\nif (!item.hasItemMeta()) return;','Supposer qu’un ItemStack existe toujours dans un PlayerInteractEvent.'],
      ['Pour un menu, annuler le clic avant de traiter le bouton évite que le joueur puisse déplacer un item qui sert d’interface. Ensuite seulement, identifie le menu et le bouton concernés.','event.setCancelled(true);\nif (event.getRawSlot() == 13) {\n    openNextMenu(player);\n}','Annuler uniquement après avoir modifié l’inventaire et provoquer des comportements étranges.'],
      ['Une ItemFactory centralise les conventions de ton plugin : matériaux, noms, lore, PDC, enchantements et flags. Si le design change, tu modifies un seul endroit.','public ItemStack createQuestKey() {\n    // construire l’item et poser son PDC\n    return new ItemStack(Material.TRIPWIRE_HOOK);\n}','Copier-coller la construction du même item dans de nombreux listeners.'],
      ['addItem retourne les piles qui n’ont pas trouvé de place. Ton plugin doit décider quoi faire : les dropper au sol, les renvoyer, ou informer le joueur. Sinon une récompense peut être perdue lorsque l’inventaire est plein.','Map<Integer, ItemStack> leftovers = player.getInventory().addItem(reward);\nleftovers.values().forEach(item -> player.getWorld().dropItemNaturally(player.getLocation(), item));','Ignorer le résultat de addItem lorsqu’un item est garanti comme récompense.'],
      ['Un bon système d’items custom relie création, identification et action. Le même identifiant PDC est écrit par ItemFactory puis lu par le listener, ce qui évite les tests basés sur le texte visible.','ItemFactory → ItemStack + PDC\nListener → lire PDC\nAbilityService → exécuter la capacité','Créer un item custom sans prévoir comment il sera reconnu plus tard.']
    ],
    ui: [
      ['Adventure représente le texte sous forme de composants. Chaque composant peut avoir sa couleur, sa décoration et des enfants. Cette composition permet de construire des messages riches sans concaténer des codes de couleur manuellement.','Component message = Component.text("Bonjour ")\n    .color(NamedTextColor.GRAY)\n    .append(Component.text(player.getName()).color(NamedTextColor.AQUA));','Mélanger partout des chaînes avec des codes de couleur historiques et des Components modernes.'],
      ['append permet de construire un arbre de composants. Pense à la lisibilité : un message long peut être construit à partir de petits composants réutilisables plutôt que d’une énorme expression.','Component prefix = Component.text("[SHOP] ").color(NamedTextColor.GOLD);\nComponent body = Component.text("Objet acheté !");\nplayer.sendMessage(prefix.append(body));','Construire du texte complexe avec des dizaines de concaténations de String.'],
      ['L’ActionBar doit rester courte, car elle disparaît facilement et peut être remplacée par une autre information. C’est parfait pour l’état courant, le cooldown ou l’objectif actif, pas pour les explications longues.','player.sendActionBar(Component.text("Compétence prête !"));','Réutiliser l’ActionBar pour afficher un paragraphe entier.'],
      ['Un Title interrompt fortement la lecture de l’écran. Utilise-le pour marquer un moment, pas comme une interface permanente. Les timings servent à contrôler son arrivée, sa présence et sa disparition.','player.showTitle(Title.title(\n    Component.text("Niveau 10"),\n    Component.text("Nouveau pouvoir débloqué")\n));','Envoyer un Title à chaque changement d’état d’un timer.'],
      ['Title.Times permet de régler finement l’animation. Une entrée courte évite de bloquer trop longtemps l’écran tandis qu’un temps de maintien plus long peut convenir à un message important.','Title.Times times = Title.Times.times(\n    Duration.ofMillis(300),\n    Duration.ofSeconds(2),\n    Duration.ofMillis(500)\n);','Mettre des timings gigantesques et rendre les Titles pénibles à utiliser.'],
      ['La BossBar est persistante tant qu’elle est visible pour le joueur. Conserve une instance et mets simplement à jour son titre ou sa progression. Un système de gestionnaire personnalisé peut s’occuper d’ajouter et retirer les joueurs.','bar.progress(Math.max(0.0, Math.min(1.0, progress)));\nbar.name(Component.text("Capture : " + percent + "%"));','Créer une BossBar par mise à jour au lieu de réutiliser l’instance.'],
      ['Les sons donnent un feedback immédiat. Les toasts, liés au système d’advancements, sont intéressants pour des récompenses ou découvertes. Une UI efficace combine ces signaux au lieu de tout afficher dans le chat.','player.playSound(player.getLocation(), Sound.ENTITY_PLAYER_LEVELUP, 1f, 1.2f);','Jouer plusieurs sons et notifications en même temps pour chaque petite action.'],
      ['Un dialogue sert surtout à présenter un choix ou une narration structurée. Sépare la construction de l’interface de la logique appelée lorsque le joueur choisit un bouton. Pour les versions récentes, vérifie l’API Paper exposée avant d’utiliser des classes internes.','DialogueManager → affiche le dialogue\nChoice → appelle QuestService\nQuestService → modifie la progression','Mélanger construction du dialogue, sauvegarde SQL et logique de quête dans une seule méthode.']
    ],
    commands: [
      ['Brigadier décrit une commande comme un arbre. Les mots fixes sont des littéraux ; les valeurs saisies par le joueur sont des arguments. Cette structure permet au moteur de connaître la syntaxe attendue avant l’exécution.','/shop buy <item> <amount>\n      root\n     /    \\\n   buy    ...\n  /  \\\nitem amount','Construire une seule grosse chaîne et analyser manuellement les arguments.'],
      ['Un littéral est une partie fixe, comme « give », « reload » ou « balance ». Les builders Brigadier servent à composer l’arbre progressivement.','LiteralArgumentBuilder.literal("shop")\n    .then(LiteralArgumentBuilder.literal("balance"));','Faire de tous les éléments des chaînes libres alors que certains sont des mots fixes.'],
      ['Les argument types valident et convertissent l’entrée. Une contrainte directement dans le type est plus fiable qu’un gros bloc de validation écrit après la conversion.','IntegerArgumentType.integer(1, 64)\n\n// accepte seulement 1..64','Accepter un entier quelconque puis tenter de réparer les valeurs invalides plus tard.'],
      ['Les noms d’arguments servent ensuite de clés dans le CommandContext. Ils doivent rester cohérents entre la déclaration et la lecture.','int amount = IntegerArgumentType.getInteger(context, "amount");','Lire « count » alors que l’argument a été déclaré sous le nom « amount ».'],
      ['Les suggestions améliorent la découverte des commandes. Elles doivent être rapides : une suggestion qui fait une requête SQL à chaque pression de TAB dégrade immédiatement l’expérience.','suggests((context, builder) -> {\n    Bukkit.getOnlinePlayers().forEach(p -> builder.suggest(p.getName()));\n    return builder.buildFuture();\n});','Faire des opérations lourdes ou bloquantes pendant chaque suggestion.'],
      ['Les permissions peuvent être posées à différents niveaux de l’arbre. Une commande publique peut avoir une branche administration protégée, sans dupliquer toute la commande.','root.then(\n    LiteralArgumentBuilder.literal("reload")\n        .requires(source -> source.getSender().hasPermission("plugin.reload"))\n);','Vérifier une permission uniquement dans une partie très profonde et oublier les autres chemins.'],
      ['La source d’une commande peut être la console, un joueur ou une autre source supportée. Ton code doit donc choisir explicitement : soit accepter toute source, soit refuser celles qui ne sont pas compatibles avec l’action.','CommandSender sender = context.getSource().getSender();\nif (!(sender instanceof Player player)) {\n    sender.sendMessage("Cette commande nécessite un joueur.");\n    return 0;\n}','Caster directement en Player sans vérifier la source.'],
      ['Un projet de commandes complet sépare la construction Brigadier de la logique métier. MainCommand construit l’arbre ; CoinService modifie les données ; le résultat final reste facile à tester et à faire évoluer.','MainCommand\n   ↓\nCoinService\n   ↓\nPlayerDataRepository','Mettre les calculs d’économie et les requêtes SQL directement dans les builders Brigadier.']
    ],
    quality: [
      ['Une permission doit représenter une capacité, pas un grade. Cela permet de créer ensuite des rôles différents dans un plugin de permissions sans changer le code du plugin.','plugin.command.reload\nplugin.command.give\nplugin.admin','Coder « if joueur == admin » à plusieurs endroits.'],
      ['Une configuration efficace sépare les réglages, les messages et éventuellement les paramètres de fonctionnalités. Donne des valeurs par défaut et documente leur rôle afin que l’administrateur comprenne immédiatement le fichier.','settings:\n  max-coins: 100000\n  combat-seconds: 10\nfeatures:\n  custom-items: true','Lire partout des chaînes magiques comme "settings.x.y" sans centraliser les valeurs importantes.'],
      ['Un MessageService peut centraliser le chargement des textes, la couleur et la traduction. Le reste du plugin demande alors simplement un message, au lieu de connaître le format YAML.','messageService.send(player, "no-permission");','Dupliquer les mêmes textes dans les listeners, commandes et services.'],
      ['Les cas limites font partie du comportement normal d’un plugin : joueur déconnecté, monde supprimé, item absent, inventaire plein, configuration incomplète. Traite ces situations avant la logique principale.','Player player = Bukkit.getPlayer(uuid);\nif (player == null) return;','Considérer null comme un bug impossible et lancer une exception au premier cas inattendu.'],
      ['La performance se raisonne en fréquence × quantité de travail. Un calcul léger chaque tick peut être acceptable alors qu’un scan complet de tous les joueurs, entités et chunks ne l’est pas.','// idée :\n// 20 ticks/s × 100 joueurs × 500 entités = beaucoup de travail\n// réduire la fréquence et la zone de recherche','Mettre toutes les tâches à 1 tick simplement « pour que ce soit fluide ».'],
      ['NMS permet des fonctionnalités plus bas niveau mais augmente le coût des mises à jour. Une classe adaptatrice isolée limite la propagation des dépendances version-spécifiques dans le reste du plugin.','Paper API\n   ↓\nNmsAdapter (si indispensable)\n   ↓\nimplémentation versionnée','Importer des classes net.minecraft.* dans tous les services du projet.'],
      ['Git doit t’aider à revenir à un état stable. Les petits commits expliquent l’évolution du plugin et rendent les régressions plus faciles à localiser.','feat: add custom item\nfix: handle full inventory\nrefactor: extract data repository','Faire un seul commit « final plugin » après plusieurs semaines de travail.'],
      ['Avant de publier, teste le cycle complet : installation, démarrage, première connexion, commandes, sauvegarde, redémarrage, permissions et nettoyage. Un plugin fiable survit à un redémarrage, pas seulement au premier test.','build → install → test → stop → restart → test again','Tester uniquement avec un serveur déjà configuré et ne jamais vérifier le redémarrage.']
    ]
  };

  function apply() {
    Object.entries(EXTRA).forEach(([courseId, entries]) => {
      const course = window.COURSES?.[courseId];
      if (!course || !Array.isArray(course.levels)) return;
      entries.forEach((extra, index) => {
        const lesson = course.levels[index];
        if (!lesson || lesson.__extraDepthAdded) return;
        lesson.text = `${lesson.text}\n\n${extra[0]}`;
        lesson.code = `${lesson.code}\n\n// Exemple approfondi\n${extra[1]}`;
        lesson.tip = `${lesson.tip}\n\n⚠️ Piège à éviter : ${extra[2]}`;
        lesson.__extraDepthAdded = true;
      });
    });
  }

  apply();
})();
